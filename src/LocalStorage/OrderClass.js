class OrderClass {
  constructor(element) {
    this._filePath = element._filePath;
    this._fileName = element._fileName;
    this._turnAroundTime = element._turnAroundTime;
    this._noOfSpeakers = element._noOfSpeakers;
    this._timeStamp = element._timeStamp;
    this._verbitam = element._verbitam;
    this._totalCost = element._totalCost;
    this._fileDuration = element._fileDuration;
    this._secondsDuration = element._secondsDuration;
  }
}
export default OrderClass;
