class DisplayAcceptedOrderClass {
  constructor(element) {
    this._orderId = element._orderId;
    this._noOfSpeaker = element._noOfSpeaker;
    this._fileName = element._fileName;
    this._trurnAroundTime = element._trurnAroundTime;
    this._timeStamp = element._timeStamp;
    this._totalCost = element._totalCost;
    this._filePath = element._filePath;
  }
}
export default DisplayAcceptedOrderClass;
